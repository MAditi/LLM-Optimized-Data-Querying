# LLM-Optimized-Data-Querying

This project focuses on building an intelligent system that uses Large Language Models (LLMs) to convert natural language questions into SQL queries. It enables users to interact with a data warehouse without requiring SQL knowledge, making data access simple, efficient, and user-friendly.


Objective:

To develop a system that converts natural language queries into SQL using LLMs, improving accessibility to data warehouses, reducing query complexity, and generating optimized queries for better performance.

Features:

Natural language to SQL conversion
Automated SQL query generation using LLM logic
Execution of queries on relational databases
Easy access to data for non-technical users
Query optimization for improved performance

Technologies Used:

Python
SQLite / MySQL / PostgreSQL,
Google Colab,
OpenAI API &
SQL

Dataset:

The project uses a sample relational dataset that includes:

Customers table,
Orders table,
Sales data &
Product information


System Workflow:

User enters a query in natural language
LLM converts the query into SQL
SQL query is executed on the database
Results are retrieved and displayed to the user
